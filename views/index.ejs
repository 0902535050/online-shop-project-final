<%- include('parts/header')%>
<%- include('parts/navbar')%>


<div class="slider">
    <div class="load">
    </div>
    <div class="content">
        <div class="principal">
            <h1>ONLINE SHOP</h1>
            <p>hú dè</p>
        </div>
    </div>
</div>

<div class="container-fluid bg-light-gray mt-5 pb-5">

    <div class="container pt-5">

        <form class="form-inline" action="/" method="GET">
            <select name="category" class="form-control" style="height: 34px">
                <option value="all">All</option>
                <option value="allProductTop">Top Model</option>
                <option value="mostWanted">Most Wanted</option>
                <option value="scarfs">Scarfs</option>
                <option value="onSale">On Sale</option>
                <option value="featured">Featured</option>
                <option value="fromTheBlog">From The Blog</option>
                <option value="trends">Trending</option>
            </select>&nbsp;
            <input type="submit" class="form-control mr-sm-2" value="Filter">
            <button class="btn btn-outline-warning my-2 my-sm-0"><a href="/">---> BACK <---</a> </button> </form> <div
                        class="row">
                        <h3>ALL PRODUCTS TOP</h3>
    </div>
    <div class="row">
        <div class="underline"></div>
    </div>
</div>

<% if(validationError) { %>
<p class="alert alert-danger">
    <%= validationError.msg%>
</p>
<% } %>

<% if(products.length === 0) {%>
<div class="alert alert-danger">
    there is no products
</div>
<% } else {%>
<div class="container mt-5">
    <div class="row">
        <%  for(let product of products)  if (product.category === "allProductTop"){   %>
        <div class="col-md-3">
            <div class="card">
                <a href="/product/<%= product._id%>"><img src="<%= product.image%>" alt="card-1"
                        class="card-img-top"></a>
                <div class="card-body">
                    <a href="/product/<%= product._id%>">
                        <h5><%= product.name%></h5>
                    </a>
                    <h6 class="price"><%= product.price%> $</h6>

                    <h6 class="description"><%= product.description%></h6>
                    <form action="cart" method="POST" class="card-img-top">
                        <input type="number" name="amount" class="form-control">
                        <input type="hidden" name="name" value="<%= product.name%>">
                        <input type="hidden" name="price" value="<%= product.price%>">
                        <input type="hidden" name="image" value="<%= product.image%>">
                        <input type="hidden" name="category" value="<%= product.category%>">
                        <input type="hidden" name="productId" value="<%= product._id%>">
                        <input type="hidden" name="redirectTo" value="/">
                        <input type="submit" value="Add to cart" class="btn btn-primary">
                    </form>

                </div>
            </div>
        </div>
        <% } } %>
    </div>
</div>



<!-- <div class="container mt-5">
    <div class="row">
        <% for(let product of products) if (product.category === "neckerChief"){%>

        <div class="col-md-3">
            <div class="card">
                <img src="<%= product.image%>" alt="card-1" class="card-img-top" style="height:;">
                <div class="card-body">
                    <h5><%= product.name%></h5>
                    <h6 class="price"><%= product.price%> $</h6>
                    <h6 class="description"><%= product.description%></h6>
                    <form action="cart" method="POST" class="card-img-top">
                        <input type="number" name="amount" class="form-control">
                        <input type="submit" value="Add to cart" class="btn btn-primary">
                    </form>
                </div>
            </div>
        </div>
        <% } %>
    </div>
</div> -->

</div>
<div class="container-fluid pt-5 pb-5">
    <div class="container">
        <div class="row">
            <div class="col-md-4">
                <div class="row">
                    <h4>MOST WANTED</h4>
                </div>
                <div class="row">
                    <div class="underline-green"></div>
                </div>
                <% for(let product of products) {
                    if (product.category === "mostWanted") { %>
                <div class="media mt-5">
                    <img src="<%= product.image%>" class="img-fluid mr-3" alt="media1">
                    <div class="media-body mt-2">
                        <h5><%= product.name%></h5>
                        <h6 class="price"><%= product.price%> $</h6>
                        <form action="cart" method="POST" class="card-img-top">
                            <input type="number" name="amount" class="form-control">
                            <input type="submit" value="Add to cart" class="btn btn-primary">
                        </form>
                    </div>
                </div>
                <% } } %>
            </div>

            <div class="col-md-4">
                <div class="row">
                    <h4>SCARFS</h4>
                </div>
                <div class="row">
                    <div class="underline-blue"></div>
                </div>
                <% for(let product of products) {
                    if (product.category === "scarfs") { %>
                <div class="media mt-5">
                    <img src="<%= product.image%>" class="img-fluid mr-3" alt="media1">
                    <div class="media-body mt-2">
                        <h5><%= product.name%></h5>
                        <h6><%= product.price%> $</h6>
                        <form action="cart" method="POST" class="card-img-top">
                            <input type="number" name="amount" class="form-control">
                            <input type="submit" value="Add to cart" class="btn btn-primary">
                        </form>
                    </div>
                </div>
                <% } } %>
            </div>



            <div class="col-md-4">
                <div class="row">
                    <h4>ON SALE</h4>
                </div>
                <div class="row">
                    <div class="underline-black"></div>
                </div>
                <% for(let product of products) {
                    if (product.category === "onSale") { %>
                <div class="media mt-5">
                    <img src="<%= product.image%>" class="img-fluid mr-3" alt="media1">
                    <div class="media-body mt-2">
                        <h5><%= product.name%></h5>
                        <h6><%= product.price%> $</h6>
                        <form action="cart" method="POST" class="card-img-top">
                            <input type="number" name="amount" class="form-control">
                            <input type="submit" value="Add to cart" class="btn btn-primary">
                        </form>
                    </div>
                </div>
                <% } } %>
            </div>
        </div>
    </div>
</div>




<div class="container-fluid bg-light-gray pt-5 pb-5">

    <div class="container mt-0">
        <div class="row">
            <h4>FEATURED</h4>
        </div>
        <div class="row">
            <div class="underline"></div>
        </div>
    </div>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-3">
                <% for(let product of products) {
                if (product.category === "featured") { %>
                <div class="card">
                    <img src="<%= product.image%>" class="img-fluid mr-3" alt="media1">
                    <div class="card-body">
                        <h4><%= product.name%></h4>
                        <h6><%= product.price%></h6>
                        <form action="cart" method="POST" class="card-img-top">
                            <input type="number" name="amount" class="form-control">
                            <input type="submit" value="Add to cart" class="btn btn-primary">
                        </form>
                    </div>
                </div>
                <% } } %>
            </div>
        </div>
    </div>
</div>





<div class="container mt-5">
    <div class="row">
        <h4>FROM THE BLOG</h4>
    </div>
    <div class="underline"></div>
</div>

<div class="container pb-5 blog">
    <div class="row">
        <div class="col-md-6">
            <% for(let product of products) {
            if (product.category === "fromTheBlog") { %>
            <div class="media mt-5">
                <img src="<%= product.image%>" class="img-fluid mr-3" alt="media1">
                <div class="media-body mt-2">
                    <h4><%= product.name%></h4>
                    <p><%= product.price%></p>
                    <p><i class="fa fa-user" aria-hidden="true"></i> admin. Date: 12-2-2018</p>
                </div>
            </div>
            <% } } %>
        </div>
    </div>

    <div class="container-fluid pt-5 pb-5 bg-light-gray">
        <div class="container">
            <div class="row">
                <h4>TRENDS</h4>
            </div>
            <div class="row">
                <div class="underline-blue"></div>
            </div>
        </div>

        <div class="container pt-5">
            <div class="row">
                <div class="col-md-3">
                    <% for(let product of products) {
                if (product.category === "trends") { %>
                    <div class="card">
                        <img src="<%= product.image%>" class="img-fluid mr-3" alt="media1">
                        <div class="card-body">
                            <h4><%= product.name%></h4>
                            <h6><%= product.price%> $</h6>
                            <form action="cart" method="POST" class="card-img-top">
                                <input type="number" name="amount" class="form-control">
                                <input type="submit" value="Add to cart" class="btn btn-primary">
                            </form>
                        </div>
                    </div>
                    <% } } %>
                </div>
            </div>
        </div>
    </div>



    <%- include('parts/footer')%>